# Configuration for the Planner module

# Ollama server URL (default assumes local Ollama instance)
OLLAMA_BASE_URL = "http://localhost:11434"

# Default model to use with Ollama (adjust to any model you have installed)
DEFAULT_MODEL = "qwen2.5:7b-instruct"
